DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=edirne_rota_db
JWT_SECRET=change_this_secret
PORT=3000

# Email servisi ayarları (SMTP)
# Gmail için örnek: smtp.gmail.com, port 587, kullanıcı adı ve "App Password"
# Diğer servisler: SendGrid, AWS SES, Mailgun vb.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@edirne.com
SMTP_FROM_NAME=Edirne Rota

# Frontend URL (şifre sıfırlama linkinde kullanılacak)
FRONTEND_URL=http://localhost:3000
